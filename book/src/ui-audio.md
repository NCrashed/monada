# UI, HUD, and audio

*This chapter is not yet written.*

It will cover the immediate-mode HUD API (`ui_clear`, `ui_image`, `ui_text`,
`ui_button`, and the animated-portrait `ui_gif`), the status line, and the
audio calls — one-shot sounds, looping sounds, background music, and the
synthesized "voice" blips the dialogue subsystem uses. All of it is
presentation, so none of it affects the simulation or a headless run.
