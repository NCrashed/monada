# Entities and models

*This chapter is not yet written.*

It will cover archetypes and entity fields, the sprite model registry
(`model_box`, KV6 sprites via `model_kv6`), and the animated directional
billboard "actors" (`model_actor`) the action-RPG uses, along with facing,
animation state, and tinting.

Until then, the [action-RPG map](https://github.com/NCrashed/monada/tree/master/crates/monada-rpg/map)
is the fullest worked example of models and actors.
