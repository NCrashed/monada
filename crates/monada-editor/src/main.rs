//! monada World Editor (DESIGN.md §3.5): terrain editor, archetype
//! editor, trigger/script editor (text + egui-snarl node graph),
//! asset import, and in-process test-play. egui + a Rust/Haskell ->
//! wasm build pipeline.
//!
//! Skeleton only — lands at **M5/M6** (DESIGN.md §7).

fn main() {
    eprintln!("monada-editor: not yet implemented (M5). See DESIGN.md §3.5, §7.");
}
