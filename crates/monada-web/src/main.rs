//! monada wasm32 host (DESIGN.md §4): same as `monada-host` but with
//! web transport (WebTransport / WebSocket fallback). Mirrors
//! `roxlap-web`.
//!
//! Skeleton only — lands alongside the M4 browser demo (DESIGN.md §7).

fn main() {
    eprintln!("monada-web: not yet implemented (M4). See DESIGN.md §7.");
}
