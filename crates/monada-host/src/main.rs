//! monada native host (DESIGN.md §4): winit + softbuffer event loop
//! wiring `monada-render` + `monada-net`. Mirrors `roxlap-host` /
//! `roxlap-cave-demo`.
//!
//! Skeleton only — the real loop lands at **M1** (DESIGN.md §7).

fn main() {
    eprintln!("monada-host: not yet implemented (M1). See DESIGN.md §7.");
}
